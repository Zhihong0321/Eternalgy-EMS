{
  "name": "eternalgy-ems-backend",
  "version": "0.1.0",
  "description": "Energy Management System - Backend API & WebSocket Server",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "db:migrate": "node src/db/migrate.js",
    "build": "npm --prefix ../frontend ci --legacy-peer-deps --include=dev && npm --prefix ../frontend run build && node ../scripts/sync-frontend.js"
  },
  "keywords": [
    "energy",
    "mqtt",
    "websocket",
    "peak-shaving"
  ],
  "author": "Eternalgy",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
